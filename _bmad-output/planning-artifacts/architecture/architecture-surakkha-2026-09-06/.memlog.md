---
scope: Surakkha v1 — lighthouse-city water-safety monitoring platform
purpose: consistency contract for the 7 PRD capabilities + 15 constraints; build-substrate + interactive deck
altitude: feature (governs epics → stories; spec is the parent, spec ARCHITECTURE-SPINE.md will be adopted as companion)
---

- (event) bmad-architecture activated
- (decision) deliverable = architecture spine + interactive HTML+SVG deck
- (decision) working mode = coaching path
- (decision) paradigm = to be elicited (user asked to be coached)
- (decision) inheriting from spec/architecture-invariants.md — that companion already names sealed-component contracts, audit chain gateway, multi-radio ingestion, channel adapters, RBAC matrix per city. Spine will adopt these as inherited invariants (ADOPTED-tagged), not re-derive them.
- (decision) paradigm = event-sourced core + CQRS read models [ADOPTED]. Rationale: audit chain becomes the source of truth by definition; simulator becomes historical event replay; audit browser becomes event replay with filters; ML training data is the event stream; sealed-component v2 swaps are projection swaps.
- (decision) Binds: all 7 capabilities (CAP-1..CAP-7) + all 15 constraints (C-1..C-16 minus C-12). Prevents: dual-writes between audit chain and operational state; "audit log" as a logging afterthought rather than the source of truth.
- (direction) next elicitation: state model and event boundaries (what are the event types, what is an aggregate root, what is a projection).
- (decision) aggregate model = hybrid [ADOPTED]. Observations (SensorReading, AnjaliReport, Complaint, LabResult, EnvSignal) are first-class events written to the stream before fusion. Incident is created lazily when fusion correlates ≥1 observation into a coherent story. After Incident creation, commands target the Incident aggregate (EscalateIncident, ExecutePlaybookStep, CaptureDeviation, ApprovePlaybookAmendment, IssueConsumerMessage). Pros: writes stay simple, reads stay focused, command boundaries clean. Cons: eventual-consistency reasoning between observation-time and incident-creation; spine must AD this.
- (decision) event-schema evolution = strict versioning + upcasters [ADOPTED]. Every event schema versioned (v1, v2, v3…). New versions add fields but never rename or remove. Old events upcasted on read by per-version upcaster functions. Projection rebuilds replay from genesis. Justification: audit chain is load-bearing (C-8 + C-13); history cannot be rewritten. Prevents event-schema drift between v1 and v2.
- (decision) projection consistency = live (sub-second lag in v1). Priya dashboard, PHA pane, fusion engine output read live projections. Simulator (FR-3.7) can opt into point-in-time replay against hypothetical playbook versions. Monthly review cluster (FR-3.6) reads live projection with bucketed materialization.
- (decision) sealed components = spec split + Incident Aggregator (added) [ADOPTED]. Components: Audit Chain Gateway, Source-Fusion Engine, Incident Aggregator (new — lazy materialization of incidents from observation stream), Playbook Engine, Channel Adapters, Multi-Radio Ingestion, Auth + RBAC, UIs (Anjali-mobile / Priya-desktop / PHA-pane). Each gets a sealed-contract AD with stable interface. v2 swap-in is a port swap.
- (decision) RBAC enforcement = defense-in-depth, 3 layers [ADOPTED]. Permission checks at (1) gateway (admission), (2) service boundary (component-internal), (3) data-access layer (storage query). The auth service is a component; the policy matrix is per-city; the enforcement is a cross-cutting concern. Prevents the "single check is load-bearing" failure mode and makes audit-chain evidence of access policy violations complete.
- (decision) tenant isolation = strict logical per-tenant [ADOPTED]. Schema-level separation per tenant city; shared cluster; no cross-tenant reads at any layer. Matches C-1 + architecture-invariants.md §1. Federation (v2) operates on aggregated roll-ups only.
- (decision) data residency = in-region for Dhaka [ADOPTED]. All Dhaka-resident data lives in Bangladesh-resident storage. Cluster in Bangladesh region (AWS ap-southeast-1 candidate, or Bangladesh-region provider if available; deferred to stack selection). Aligns with C-10 + PRD §5.4.
- (decision) multi-radio contract enforced at gateway [ADOPTED]. Every sensor ingest request declares a radio tier (cellular/LPWAN/SD-card); the gateway routes through the right adapter. Adapters are port-swappable (C-15). Sensor silence beyond configurable threshold = audit event (per spec §3 of detection-layer.md).
- (decision) deployment topology = single-region Dhaka for v1 [ADOPTED]. Audit-chain gateway writes to local + single replicated peer. WB evidence export runs as scheduled job. Cheapest, matches "single city in v1" (NG-5). Multi-region is a v2-on upgrade when federation activates.
- (decision) observability = internal stack [ADOPTED]. Structured logs + metrics + traces; chain-integrity monitor (verify chain hash every N min); sensor-silence monitor (alert when any sensor silent > threshold); override-anomaly monitor (logged v1, enforced v2 per spec architecture-invariants.md §7). Vendor-neutral stack to fit WB procurement rules (no proprietary lock-in for an audit-chain-bound platform).
- (event) elicitation closed; ready to distill spine
- (event) spine distilled — 12 ADs initial. Paradigm = event-sourced core + CQRS read models; hybrid aggregate model; sealed components (with Incident Aggregator added); strict versioning + upcasters; strict per-tenant isolation; defense-in-depth RBAC; three UIs as products; multi-radio enforced at gateway; single-region Dhaka; internal vendor-neutral observability; two-person rule; liability split via actor identity.
- (event) lint_spine.py run by hand (bash blocked at sandbox). Verdict: clean mechanically. AD-1..AD-12 monotonic; all ADs have Binds/Prevents/Rule; no high-severity placeholders; Stack table TBD rows are explicit "see Deferred" cross-references (low severity by lint rule).
- (event) reviewer gate dispatched in parallel — web-verification, adversarial, rubric-walker. All three returned PASS-with-notes.
- (decision) AD-2 tightened. Incident identity is content-addressed: `sha256(canonical(observation_event_ids[] | tenant_id | correlation_window_id | contaminant_class))`. Incident Aggregator is sole writer of `IncidentCreated`. Correlation window lives in platform-config and is shared between Fusion and Aggregator.
- (decision) AD-3 tightened. Sealed list now includes Simulator Harness with non-persistence property. Fusion contract's source_attribution is pinned to observation_id list shape (not free-form strings).
- (decision) AD-4 tightened. Schema-version bumps require dual sign-off (vendor + pha_approver) — two-person rule applied to schema evolution. Upcasters are owned by the event-type owner and shipped in lockstep with the version bump; chain-integrity monitor flags lagging projections.
- (decision) AD-11 tightened. Both signatures must attest to identical `(event_id, payload_hash, version_id)` triple — pattern is `SignatureAttestation` events that reference each other; gateway accepts the underlying command only after both attestations with matching triples.
- (decision) AD-12 tightened. `actor_identity.role` is a closed enum tied to per-tenant RBAC matrix (`{vendor, pha_approver, utility_operator, anjali, priya, pha_viewer, system}`). `actor_identity.ref` is resolved from auth-service session token, not minted by emitting component. Gateway **rejects** at write-time — split is structural, not contractual.
- (decision) new AD-13 — Playbook version-of-record is content-addressed via `PlaybookVersionPublished` event; `version_id = sha256(canonical(serialized_playbook_definition))`. Lineage is linear with `parent_version_id`. Drafts emit `PlaybookVersionDrafted`; abandoned drafts emit `PlaybookVersionAbandoned` (append-only). Simulator Harness reads inputs from event stream, not projections — non-persistent per AD-3.
- (decision) new AD-14 — Idempotency by `(tenant_id, event_id)`. Gateway rejects duplicate pairs with `DuplicateEventRejected` (silent dedup is forbidden — the chain must show the attempt). Adapters mint event_id at user-acknowledged submission time, retry reuses same event_id. Closes SD-card store-and-forward and mobile reconnect storm paths.
- (decision) new AD-15 — Change-management authority structured via dual-signed events for (a) schema bump, (b) playbook promotion draft→published, (c) city-config change. Authority + sign-off roles named per category. All changes are event-sourced and replayable; no out-of-band config-update path.
- (decision) new AD-16 — Retention floor + RTBF mechanism. Chain remains append-only and tamper-evident. Right-to-be-forgotten honored by `SubjectRedacted` events; projections apply redaction on read. Retention floor (default 7 years per WB / city contract) enforced by chain-integrity monitor. Dual principle: chain remembers everything; projection forgets what the law requires.
- (decision) AD-9 / Stack amended. AWS ap-southeast-1 demoted to replicated-peer role; active write path requires Bangladesh-resident provider (local providers under evaluation). WB evidence export may not export raw PII or raw observations out of Bangladesh; only aggregated k-anonymized indicators.
- (decision) observability stack named. OpenTelemetry (CNCF Graduated) for instrumentation; vendor-neutral metrics/logs/traces backend per AD-10.
- (decision) Simulator Harness ratified as a sealed component in AD-3 (non-persistence property) and AD-13 (reads from event stream).
- (decision) FR-5.6 (auto-PHA-monthly-report is a projection of the audit trail) ratified in Capability Map → AD-13 + AD-16 + C-16.
- (decision) Capability Map extended: Simulator (FR-3.7), PHA monthly report (FR-5.6), WB evidence export (FR-6.6) rows added.
- (decision) Consistency Conventions extended: version-of-record rule, idempotency rule, chain-hash-computation-by-gateway rule, redaction rule.
- (decision) Dependency Direction diagram + Container view updated: Simulator added as sealed component, RBAC dot-enforced into SIM, EVT feeds SIM directly (simulator reads from event stream).
- (event) spine has 16 ADs after reviewer-driven amendments
- (event) spine is `status: final`, ready for human-facing deck
- (event) interactive HTML+SVG deck built at ARCHITECTURE-DECK.html — paradigm walk-through, AD table with click-through to full text, three diagrams (dependency / container / deployment) as inline SVG, consistency conventions, capability→architecture map, deferred-with-revisit-trigger. Single self-contained file.
- (event) spine finalized
