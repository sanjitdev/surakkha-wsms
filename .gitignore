# Dependencies
node_modules/
.pnpm-store/

# Build artifacts
dist/
build/
*.tsbuildinfo

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Local env
.env
.env.local
.env.*.local

# pnpm
.pnpm-debug.log*

# Project-specific: BMAD skill scratch (out of repo scope)
skills/

# WDS machine-local session state (per WDS glossary: progress/ vs _progress/)
# `_progress/` is tracked (project outline + design log).
# `progress/` is machine-local (saga/freya/mimir session stubs).
progress/

.tmp/