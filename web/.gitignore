node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/
.idea/

# Playwright artifacts — generated on every test run. CI uploads
# them via the E2E workflow (traces on failure, HTML report always)
# for debugging.
test-results/
playwright-report/
blob-report/
.playwright/
